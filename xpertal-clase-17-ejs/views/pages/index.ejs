<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head') %>
  <body>
    <h1>Plantillas con EJS</h1>

    <% if (locals.age && age > 17) { %>
    <p>es mayor</p>
    <% } %>

    <!-- <p><%= locals.age ? 'si':'no' %></p> -->

    <ul>
      <% skills.forEach(skill => { %>
      <li><%= skill %></li>
      <% }) %>
    </ul>
  </body>
</html>
